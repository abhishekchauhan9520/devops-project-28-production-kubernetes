apiVersion: v1
kind: Service
metadata:
  name: production-app
  namespace: production-demo
spec:
  selector:
    app: production-app
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
